# aws_environment = "sandbox" on local machine, set via environment variable
# aws_environment = "development" in CI/CD, set via workflow input
aws_project = "aws-network-speciality"
aws_region  = "eu-west-2"
aws_service = "vpc"
